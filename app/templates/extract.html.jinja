{% extends "base.html.jinja" %}
{%block content%}
        <h2>Ekstrakcja opinii</h2>
<form action="{{url_for('extract')}}" method="POST" class = "container" >
        <div class="row g-3 align-items-center">
                <div class="col-auto">
                        <label for="product_id class="col-form-label">Kod produktu</label>
                </div>
                <div class="col-auto">
                        <input type="password" name="product_id" id="product_id" class="form-control" aria-describedby="product_idHelpInline" placeholder="product_id" required>
                </div>
                <div class="col-auto">
                        <span id="product_idHelpInline" class="form-text">
                        Kod produktu musi zawierać 5-10 cyfr.
                        </span>
                </div>
                <div class="col-auto">
                        <button type="submit" class="btn btn-primary mb-3">Confirm identity</button>
                </div>
        </div>
</form>
        {%if error is defined %}
    
    {%end block%}
{%endblock%}